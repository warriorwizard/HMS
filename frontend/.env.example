# ProxoHMS frontend environment template.
# Only variables prefixed with NEXT_PUBLIC_ are exposed to the browser.

NEXT_PUBLIC_PROXOHMS_APP_NAME=ProxoHMS
NEXT_PUBLIC_PROXOHMS_ENVIRONMENT=local
NEXT_PUBLIC_PROXOHMS_API_BASE_URL=http://127.0.0.1:8000/api/v1
